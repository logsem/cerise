(executable
 (name        interpreter)
 (modules     interpreter)
 (libraries   libinterp))

(executable
 (name      interactive)
 (modules   interactive)
 (libraries libinterp notty notty.unix containers))
